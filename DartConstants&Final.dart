void main(){
  // var ব্যবহার করলে ভ্যারিয়েবলের মান পরিবর্তন করা যায়, কিন্তু const এবং final ব্যবহার করলে মান পরিবর্তন করা যায় না।
  
  // 'var' দিয়ে একটি ভ্যারিয়েবল তৈরি করা হয়, যার মান পরে পরিবর্তন করা সম্ভব।
  // var thisYear = 2024; // এখানে 'thisYear' এর মান ২০২৪ দেওয়া হয়েছে।
  // thisYear = 2025; // পরে আমরা 'thisYear' এর মান ২০২৫ সেট করেছি।
  // print(thisYear); // এখানে ২০২৫ প্রিন্ট হবে।

  // এখানে 'const' ব্যবহার করা হয়েছে। 'const' দিয়ে দেওয়া ভ্যারিয়েবলের মান একবার সেট করার পর তা পরিবর্তন করা সম্ভব নয়।
  // const likeYear = 2024; // এখানে 'likeYear' এর মান ২০২৪ সেট করা হয়েছে।
  // likeYear = 2025; // এই লাইনটি রান করলে এরর হবে, কারণ 'const' মান পরিবর্তনযোগ্য নয়।
  // print(likeYear); // এই লাইনটি আসবে না, কারণ উপরের লাইনেই এরর হবে।

  // 'final' ব্যবহার করলে ভ্যারিয়েবলের মান একবার সেট হওয়ার পর তা পরিবর্তন করা সম্ভব নয়, তবে 'final' একটি runtime constant।
  // final nextYear = 2024; // এখানে 'nextYear' এর মান ২০২৪ সেট করা হয়েছে।
  // nextYear = 2025; // এখানে রান করলে এরর হবে, কারণ 'final' দিয়ে দেওয়া ভ্যারিয়েবলের মান পরিবর্তনযোগ্য নয়।
  // print(nextYear); // এই লাইনটি আসবে না, কারণ উপরের লাইনেই এরর হবে।

  // এখন 'final' এবং 'const' ব্যবহার করলে মান পরিবর্তন করা যাবে না, এবং এ কারণে এটি রানটাইম এরর দেবে।
}
